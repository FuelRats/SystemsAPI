LOAD CSV
    FROM 'systems.csv'
    INTO {{db_uri}}
    TARGET TABLE systems_new

    WITH skip header = 1,
            fields optionally enclosed by '"',
            fields escaped by backslash-quote,
            fields terminated by ',',
            on error resume next
    SET work_mem to '{{work_mem}}', maintenance_work_mem to '{{maintenance_work_mem}}';
